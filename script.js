const fs = require('fs'); const files = ['src/app/(main)/eventos-hse/page.tsx', 'src/app/(main)/inasistencias/page.tsx', 'src/app/(main)/novedades/page.tsx', 'src/app/(main)/permisos-trabajo/page.tsx']; files.forEach(f => { let txt = fs.readFileSync(f, 'utf8'); txt = txt.replace(/onValueChange=\{\(v\) => setFormData\(\{\.\.\.formData, ([a-zA-Z0-9_]+): v\}\)\}/g, 'onValueChange={(v) => setFormData({...formData, 1: v || \\'\\'})}'); fs.writeFileSync(f, txt); })
